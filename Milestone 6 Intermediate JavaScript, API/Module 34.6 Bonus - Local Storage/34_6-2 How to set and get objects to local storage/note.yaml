Local Storage শুধুমাত্র string সাপোর্ট করে। তাই Object বা Array সেভ করার আগে
JSON.stringify() ব্যবহার করে string বানাতে হয়, আর পড়ার সময় JSON.parse()
দিয়ে আবার Object/Array এ কনভার্ট করতে হয়।


✅ Object Store & Access in Local Storage
// Object তৈরি
let user = {
  name: "Shahriar",
  age: 24,
  country: "Bangladesh"
};

// Object কে String এ কনভার্ট করে Save করা
localStorage.setItem("user", JSON.stringify(user));

// Local Storage থেকে Read করা
let getUser = JSON.parse(localStorage.getItem("user"));

console.log(getUser.name);    // Output: Shahriar
console.log(getUser.age);     // Output: 24
console.log(getUser.country); // Output: Bangladesh




✅ Array Store & Access in Local Storage
// Array তৈরি
let fruits = ["Mango", "Banana", "Apple"];

// Array কে String এ কনভার্ট করে Save করা
localStorage.setItem("fruits", JSON.stringify(fruits));

// Local Storage থেকে Read করা
let getFruits = JSON.parse(localStorage.getItem("fruits"));

console.log(getFruits);      // Output: ["Mango", "Banana", "Apple"]
console.log(getFruits[0]);   // Output: Mango
console.log(getFruits[1]);   // Output: Banana




👉 Rule of thumb:

Save করার সময় → JSON.stringify()
Access করার সময় → JSON.parse()